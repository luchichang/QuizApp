import 'package:flutter/cupertino.dart';

class Utils {
  SizedBox spacerSizedBox({double height = 0.0, double width = 0.0}) {
    return SizedBox(
      height: height,
      width: width,
    );
  }
}
